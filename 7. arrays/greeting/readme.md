# Simple Array Words

This is a basic C program that demonstrates how to store and print words using a string array.

## What the Program Does
- Creates an array of two strings.
- Stores `"HI!"` and `"BYE"` inside the array.
- Prints the first word to the screen.

## Key Concepts Used
- String arrays in C
- Indexing array elements
- Printing with `printf()`
- CS50’s `string` type
