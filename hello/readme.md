# Hello, <name> — Greeting Program in C

This program asks the user for their name and then prints a personalized greeting such as:

Hello, Kev


It uses the **CS50 Library** (`cs50.h`) to collect input from the user.

---

## 📁 File Included
- `hello.c` — The source file for this program

---

## 🧠 What This Program Demonstrates
- Using `#include <cs50.h>` for input  
- Using `#include <stdio.h>` for output  
- Getting text input with `get_string()`  
- Using variables of type `string`  
- Printing formatted output using `printf()`  

---

## 🛠️ How to Compile and Run (CS50.dev)

In the CS50 IDE:

```bash
make hello
```
Then run:
```bash
./hello
```
Sample Interaction
Name: Kev
Hello, Kev


This is one of my first interactive C programs.
It helped me understand how to take user input, store it in a variable,
and combine it with printed text to create a customized output.